---
const { btnLink, btnClass, btnText, btnAria } = Astro.props;
---

<div class=`${btnClass} hover-scale` aria-label={btnAria ? btnAria : 'Contact Indivirtus'}>
  <a href={btnLink}>{btnText}</a>
</div>
