---
import Layout from '@/layouts/Layout.astro';

import AboutFull from '@/components/aboutus/AboutFull.astro';
import Leadership from '@/components/aboutus/Leadership.astro';

// Importing the SEO
import seoData from '@/data/js/seo/about-us.js';

const { meta, faqSchema, gtm, pageLink, schema } = seoData;
---

<Layout
  seo={{
    title: `${meta.title} - Indivirtus`,
    description: meta.description,
    keywords: meta.keywords,
    canonical: meta.canonicalUrl,
    url: pageLink,
    schema: schema,
    faqSchema: faqSchema,
    ogTags: meta.ogTags,
    twitterCard: meta.twitterCard,
  }}
  gtm={gtm}
>
  <AboutFull />
  <Leadership />
</Layout>
