---
import Layout from '@/layouts/Layout.astro';

import SecurityAndConfidentiality from '@/components/aboutus/SecurityConfidentiality.astro';

// Importing the SEO
import seoData from '@/data/js/seo/aboutus/securityConfidentiality.js';

const { meta, faqSchema, gtm, pageLink, schema } = seoData;
---

<Layout
  seo={{
    title: `${meta.title} - Indivirtus`,
    description: meta.description,
    keywords: meta.keywords,
    canonical: meta.canonicalUrl,
    url: pageLink,
    schema: schema,
    faqSchema: faqSchema,
    ogTags: meta.ogTags,
    twitterCard: meta.twitterCard,
  }}
  gtm={gtm}
>
  <SecurityAndConfidentiality />
</Layout>
